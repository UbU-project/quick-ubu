# quick-ubu v1 spec

Placeholder: the operator did not supply the full v1 schema for this ticket.
The authoritative contract implemented by the `core` crate is the "Decision
context" section inlined in the C-1 ticket prompt (`C-1_prompt.md`).
